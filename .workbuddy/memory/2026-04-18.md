# 2026-04-18

## MEYieldVault DApp UI 设计原型（最新版）
- 输出文件：c:/Users/34481/WorkBuddy/20260418032710/mevault-ui-design.html
- 设计风格：纯白底色、高级简约风（Linear/Notion 风格），蓝/绿/橙/紫语义色
- 用户要求：未连接钱包时全灰（saturate(0)），连接后恢复彩色，支持点击切换状态
- 页面模块（7页）：金库总览、我的收益、销毁代币、邀请系统、质押管理、数据看板、金库设置
- 实现：CSS filter + .wallet-disconnected/.wallet-connected 类切换；销毁预览实时计算

## mevault-helper-final-interfaces ZIP 分析
- 解压路径：E:/xiazai/mevault-helper-extracted/
- 核心合约：MEYieldVault、BurnReferralDistributor、MEYieldVaultFactory、MEYieldVaultLens
- 架构：税代币 BNB 收税 → 自动质押 Lista → 得到 slisBNB → 三分（营销40%、销毁分红40%、持币分红20%）
- 新增：Lens 合约（globalStats/userStats/inviteStats/marketingStats/burnGlobalStats）
- 部署顺序：BurnReferralDistributorDeployer → MEYieldVaultDeployer → MEYieldVaultFactory
- vaultType 改为 "TaxVaultUnified"，overview 扩展至 13 项输出

## UI 更新 - 动态代币名称与邀请链接
- Logo 和标题现在从链上动态读取代币 symbol（如 REMIX → 显示 REMIXVault）
- 邀请链接自动生成带钱包地址的格式：`?ref=0x...`
- 通过邀请链接进入时自动解析 URL 参数并填充邀请人地址
- 页面结构调整：机制说明 → 官方链接 → 公告

## 合约配置更新
- 营销钱包地址：`0xf25635ec0f3ca460043d9f2abb49caacaa0328e6`
- 金库合约 ABI 已集成，支持 `stakePendingTax()` 触发质押功能
- 所有合约地址和 ABI 已对齐最新接口文档

## 全面假数据审查与修复（11项）
- 数据看板页：4个全局统计（总销毁量/总奖励/参与地址/通缩率）全部改为动态读取
- 数据看板页：营销钱包 `892.45` → 动态从 Lens 读取；4个合约地址假截断 → 使用真实配置
- 收益页：`$1,247.50` 初始值 → `--`，连接钱包后动态计算
- 销毁页：余额 `50,000 REMIX` → 动态读取用户代币余额
- 销毁页：setMaxBurn() 硬编码 `50000` → 读取实际余额
- 销毁页：排名 `#12` → 动态显示权重数值
- 邀请页：激活门槛 `50万/100万 + 50%进度条` → 全部动态化，初始为 `--`
- 公告系统：修复 `CURRENT_TOTAL_BURN` / `TODAY_BURN` 未定义导致公告系统完全失效
- 社区页营销钱包 USD 等价金额从未更新 → 新增 `updateMarketingUsdValue()` 函数
- slisBNB 价格：从固定 `1.0` 改为先尝试 GeckoTerminal API 获取真实价格，失败才回退

## UI 大改 - 深粉绿渐变配色 + 功能修复（第2轮）
- **全局CSS大改**：背景渐变加深饱和度（#fce7f3→#d8f5dc→#a7e8c9），卡片增加顶部粉绿条纹光晕+更强阴影
- **按钮增强**：主按钮加动态渐变动画（gradientShift）、更深的粉色阴影、disabled状态独立样式
- **头部/导航**：backdrop-filter blur提升至20px，加border和box-shadow
- **手动刷新按钮**：头部新增刷新按钮（🔄图标+旋转动画），点击触发refreshAllData()，带防抖和toast提示
- **排行榜修复**：查询范围扩大至10万块（约3天数据），展示前5名（原3名），加奖牌图标🥇🥈🥉，当前用户高亮"(我)"，缓存机制防加载失败白屏，销毁后延迟刷新传入totalBurned参数
- **claimReward重写**：领取前预检pending数量为0时给用户友好提示（不是简单报错），按钮loading状态，详细revert原因解析（OnlyVault/ZeroAmount/TransferFailed等），区分用户取消/余额不足/合约回滚等场景
- **claimAllRewards增强**：每项单独预检+结果明细报告
- **底部导航中间按钮**：加粗描边stroke-width 2.5、白色ring光环、粉色阴影shadow-pink-500/20

## 收益页新增 WBNB 持币分红 (1%) 卡片
- **收益页从3张卡片扩展为4张**：WBNB持币分红(1%, 蓝色) → slisBNB持币分红(20%, 琥珀色) → 销毁分红(粉红) → 邀请返佣(薄荷色)
- **VAULT_ABI扩展**：新增 `pendingWbnbHolderReward()` 和 `claimWbnbHolderReward()` 接口
- **chainData扩展**：`userRewards` 新增 `holderWbnb` 字段
- **loadUserStats更新**：同步读取 WBNB 待领奖励并更新 `#holderWbnbAmount` 元素
- **总奖励计算**：4项求和 (WBNB持币 + slisBNB持币 + 销毁 + 邀请)
- **claimReward新增 holderWbnb 分支**：预检查 pending > 0 后调用 vault.claimWbnbHolderReward()
- **claimAllRewards升级**：一键领取增加第3步 WBNB 持币分红领取
- **首页分配比例图**：顶部新增 "WBNB 持币分红 1%" 行
- ⚠️ 合约方法名待确认：pendingWbnbHolderReward / claimWbnbHolderReward

## 质押阈值调整
- 自动质押阈值从 **0.1 BNB** 降至 **0.01 BNB**（3处同步修改：UI显示文字、进度条提示、STAKE_CONFIG.threshold）

## 🔧 合约接口全面修正（基于 BscScan 验证）
- **根因分析**：通过 WebFetch 抓取 BscScan Vault 合约页面确认了完整接口列表
- **关键纠正**：
  - `vault.claim()` 实际 = **slisBNB 持币分红 20%**（之前误认为领 WBNB）
  - **WBNB 1% 不在 Vault 上**，通过独立的 `holderDividendContract()` 分红合约发放
  - 燃烧/邀请返佣有 Vault 级快捷方法 `claimBurnReward()` / `claimInviteReward()`
- **VAULT_ABI 重写**：移除不存在的 pendingWbnbHolderReward/claimWbnbHolderReward，新增 pendingClaim/claimBurnReward/pendingBurnReward/claimInviteReward/pendingInviteReward/holderDividendContract
- **新增 WBNB_DIVIDEND_ABI**：claim + pendingOf（通过 vault.holderDividendContract() 获取地址）
- **loadUserStats() 改用 Vault 直接查询**（更实时），Lens 仅做 tokenBalance 等辅助数据兜底
- **claimReward / claimAllRewards 全部重写**为正确的合约方法调用
- **triggerStake() 增加延迟刷新机制**：立即刷新 → 5秒后 → 12秒后（解决 Lens 缓存延迟）

## 19:35 — 用户进入 DApp 默认 BSC 链
在 `connectWallet()` 中添加了 BSC 链自动检测与切换逻辑：
- 连接钱包后立即检查 `eth_chainId`
- 非 BSC（0x38）时调用 `wallet_switchEthereumChain` 切换
- 如果链未添加（error 4902），自动调用 `wallet_addEthereumChain` 添加 BSC 主网
- 切换链后重新初始化 provider 确保后续操作正确

同时在 `DOMContentLoaded` 中添加了页面加载时的静默链检测（不弹连接请求，只静默尝试切到 BSC）

## 19:37 — 社区页数据 + 销毁页 + 分配比例图 全面修复（6项）
1. **社区总销毁量**：确认 `loadBurnDistributorStats()` → `distributor.totalActualBurned()` → 更新 `communityTotalBurn` 路径正确
2. **持币地址不显示**：BscScan API key 是占位符 `"YourApiKeyToken"` → 改为无 key 免费请求
3. **市值不显示**：增加主动调用 `loadTokenSupply()` 确保 totalSupply 已赋值后再算市值
4. **销毁页余额 + 最大按钮**：核心 bug！`formatCN()` 输出如 "5万"，`setMaxBurn()` 用 parseFloat("5万") 得 NaN → 新增 `_userRawTokenBalance` 变量保存原始数值供"最大"按钮直接使用
5. **收益分配比例图**：
   - 圆圈文字从 "slisBNB" → "金库质押收益"（两行居中显示）
   - 新增顶部 ZRT工厂 → 质押金库 99% 行（绿色渐变圆点 + 说明文字）
